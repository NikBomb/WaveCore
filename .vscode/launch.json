{
    "version": "0.2.0",
    "configurations": [
        {  "type": "by-gdb",
            "request": "launch",
            "name": "Launch tests",
            "program": "${workspaceRoot}/build/gcc14-debug/tests/wavecore_tests",
            "cwd": "${workspaceRoot}"
        }
    ]
}